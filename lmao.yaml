name: Run creal.py on website visit

on:
  push:
    branches:
      - main  # Replace with your main branch name or trigger event

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run creal.py
        run: python creal.py  # Replace with the appropriate command

